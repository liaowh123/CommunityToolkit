<UserControl x:Class="CommunityToolkit示例.View.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommunityToolkit示例.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                     Background="{TemplateBinding Background}"
                     BorderBrush="{TemplateBinding BorderBrush}"
                     BorderThickness="{TemplateBinding BorderThickness}"
                     CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1C6EA4"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButtonStyle" BasedOn="{StaticResource PrimaryButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="#FF2ECC71"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF27AE60"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WarningButtonStyle" BasedOn="{StaticResource PrimaryButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="#FFF39C12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFD68910"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" BasedOn="{StaticResource PrimaryButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="#FFE74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFC0392B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题和按钮 -->
        <Border Grid.Row="0" Padding="10" Background="#ECF0F1" CornerRadius="5" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#2C3E50">
                        <Run Text="👥 "/>
                        <Run Text="用户权限管理"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="➕ 新增用户" 
                            Command="{Binding AddUserCommand}"
                            Style="{StaticResource SuccessButtonStyle}" Margin="0,0,5,0"/>
                    <Button Content="🗑 删除用户" 
                            Command="{Binding DeleteUserCommand}"
                            Style="{StaticResource DangerButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 用户列表和权限配置 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 用户列表 -->
            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="用户列表" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <ListBox ItemsSource="{Binding Users}" 
                             SelectedItem="{Binding SelectedUser}"
                             DisplayMemberPath="DisplayName">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="10,5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#f0f8ff"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- 权限配置 -->
            <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="权限配置" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock Margin="0,0,0,15">
                        <Run Text="当前用户:"/>
                        <Run Text="{Binding SelectedUser.DisplayName}" FontWeight="Bold"/>
                        <Run Text="("/>
                        <Run Text="{Binding SelectedUser.Id}"/>
                        <Run Text=")"/>
                    </TextBlock>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <CheckBox Content="系统配置" IsChecked="{Binding SelectedUser.CanConfigureSystem}" Margin="0,0,0,5"/>
                            <CheckBox Content="PLC操作" IsChecked="{Binding SelectedUser.CanOperatePLC}" Margin="0,0,0,5"/>
                            <CheckBox Content="数据查看" IsChecked="{Binding SelectedUser.CanViewData}" Margin="0,0,0,5"/>
                            <CheckBox Content="报表导出" IsChecked="{Binding SelectedUser.CanExportReports}" Margin="0,0,0,5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <CheckBox Content="用户管理" IsChecked="{Binding SelectedUser.CanManageUsers}" Margin="0,0,0,5"/>
                            <CheckBox Content="报警确认" IsChecked="{Binding SelectedUser.CanConfirmAlarms}" Margin="0,0,0,5"/>
                            <CheckBox Content="远程控制" IsChecked="{Binding SelectedUser.CanRemoteControl}" Margin="0,0,0,5"/>
                            <CheckBox Content="系统维护" IsChecked="{Binding SelectedUser.CanMaintainSystem}" Margin="0,0,0,5"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <Button Content="💾 保存权限" 
                                Command="{Binding SavePermissionsCommand}"
                                Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0"/>
                        <Button Content="🔄 恢复默认" 
                                Command="{Binding ResetPermissionsCommand}"
                                Style="{StaticResource WarningButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</UserControl>
